"""Shared Spectra primitives.

This package is the target home for code currently shared across API, AI,
scheduler, and worker runtimes.

Import concrete modules explicitly, e.g. ``from spectra_common.constants import ...``.
"""
