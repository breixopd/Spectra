# =============================================================================
# Spectra AI Service Dependencies
# =============================================================================
# Minimal set for the AI microservice (LLM routing, embeddings, RAG).
# Extends base deps shared by all Python services.

# Core Framework
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic[email]==2.13.3
pydantic-settings==2.14.0

# Database
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0

# AI / LLM
openai==2.33.0
tensorzero==2026.4.1
fastembed==0.8.0

# HTTP client (gateway, health checks)
httpx==0.28.1

# JSON repair (LLM output cleanup)
json-repair==0.59.5

# Prompt templates
jinja2==3.1.6

# Auth (needed by shared core config)
PyJWT[crypto]==2.12.1
bcrypt==5.0.0
cryptography==47.0.0

# Utilities
python-dotenv==1.2.2
orjson==3.11.8
aiofiles==25.1.0

# Transitive deps (via app.services.tools imported by AI agents)
jsonschema==4.26.0
docker==7.1.0
defusedxml==0.7.1

# Transitively needed by shared app code imports
starlette==1.0.0
python-multipart==0.0.27
websockets==16.0
